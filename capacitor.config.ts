import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ECF-react-ionic-front',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
